import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Successful Start',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
